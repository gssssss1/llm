/**
 * Persistence layer for data storage and retrieval.
 * <p>
 * This package contains repositories, JPA entities, and data access logic.
 * It handles all interactions with the database and provides an abstraction
 * layer between the domain/engine and the underlying data storage.
 */
package com.project.backend.persistence;
