# 动态执行图使用指南

## 功能介绍

动态执行图是一个实时可视化功能，它能够在Graph执行过程中动态地展示节点的执行状态。与静态Mermaid图不同，动态执行图会：

1. **按需渲染** - 只有当节点开始执行时才会显示
2. **实时状态** - 通过颜色和动画显示节点的当前状态
3. **动态边** - 在节点完成后显示连接到下一个节点的边
4. **流式展示** - 让你清晰看到执行流程

## 界面说明

### 切换视图

在Graph详情页面，你会看到两个标签：

- **静态图** - 显示完整的Graph结构（Mermaid图）
- **动态执行图** - 显示实时执行流程

点击"动态执行图"标签即可切换到动态视图。

### 动态图区域

动态图包含以下元素：

#### 1. 画布区域
- 网格背景
- 节点以卡片形式显示
- 节点之间用箭头连接

#### 2. 图例
- 🔵 **蓝色** - 节点正在执行中
- 🟢 **绿色** - 节点执行完成
- 🔴 **红色** - 节点执行失败
- ⚫ **灰色** - 节点等待执行

#### 3. 重置按钮
- 点击可清空动态图，准备新的执行

## 节点状态

### 执行中（Running）
```
┌─────────────────┐
│  🔵 ●           │  蓝色渐变背景
│   [play icon]   │  播放图标旋转
│   node_name     │  节点名称
│  ═══════════    │  进度条动画
└─────────────────┘
```
- 蓝色渐变背景
- 播放图标持续旋转
- 底部进度条来回移动
- 外发光效果（脉冲）

### 已完成（Completed）
```
┌─────────────────┐
│  🟢 ●           │  绿色渐变背景
│  [check icon]   │  对勾图标
│   node_name     │  节点名称
│  ═══════════    │  绿色进度条
└─────────────────┘
```
- 绿色渐变背景
- 对勾图标
- 实心绿色进度条

### 失败（Failed）
```
┌─────────────────┐
│  🔴 ●           │  红色渐变背景
│   [x icon]      │  错误图标
│   node_name     │  节点名称
│  ═══════════    │  红色进度条
└─────────────────┘
```
- 红色渐变背景
- X图标
- 实心红色进度条

### 待执行（Pending）
```
┌─────────────────┐
│  ⚫ ○           │  灰色背景
│  [circle icon]  │  圆圈图标
│   node_name     │  节点名称
│  ───────────    │  灰色进度条
└─────────────────┘
```
- 浅灰色背景
- 空心圆圈图标
- 灰色进度条

## 边类型

### 静态边（Static Edge）
```
nodeA ──────────→ nodeB
      灰色实线箭头
```

### 条件边（Conditional Edge）
```
nodeA - - - - - → nodeB
      橙色虚线箭头
```

### 并行边（Parallel Edge）
```
nodeA · · · · · → nodeB
      蓝色点线箭头
```

## 使用流程

### 1. 打开Graph详情页
```
http://localhost:8080/graph/{graphId}
```

### 2. 切换到动态执行图
点击"动态执行图"标签

### 3. 执行Graph
在右侧面板：
- 输入初始状态（JSON格式）
- 点击"Execute"按钮

### 4. 观察执行
动态图会实时显示：
- 节点逐个出现
- 状态颜色变化
- 边的动态添加

### 5. 查看结果
执行完成后：
- 所有执行过的节点显示为绿色
- 失败的节点显示为红色
- 完整的执行路径可见

## 示例场景

### 场景1：简单线性流程
```
执行前: [空白画布]

步骤1: start节点出现（蓝色）
步骤2: start完成（绿色）→ process节点出现（蓝色）
步骤3: process完成（绿色）→ end节点出现（蓝色）
步骤4: end完成（绿色）

执行后: start → process → end（全绿）
```

### 场景2：条件分支
```
执行前: [空白画布]

步骤1: analyze节点出现（蓝色）
步骤2: analyze完成（绿色）
       → 根据条件，high_value或low_value出现
步骤3: 选中的路径节点执行（蓝色）
步骤4: 完成并连接到finalize
步骤5: finalize执行完成

执行后: analyze → high_value → finalize（全绿）
       或
       analyze → low_value → finalize（全绿）
```

### 场景3：并行执行
```
执行前: [空白画布]

步骤1: prepare节点出现（蓝色）
步骤2: prepare完成（绿色）
步骤3: task_a, task_b, task_c同时出现（全蓝）
步骤4: 三个任务逐个完成（变绿）
步骤5: merge节点出现（蓝色）
步骤6: merge完成（绿色）

执行后:      ┌→ task_a →┐
       prepare─→ task_b ─→ merge
             └→ task_c →┘
       （全绿）
```

## 动画效果

### 节点出现
- 从小到大的缩放动画（scale 0.8 → 1.0）
- 透明度渐变（opacity 0 → 1）
- 持续时间：300ms

### 状态变化
- 背景颜色平滑过渡
- 图标切换动画
- 进度条更新

### 边出现
- 从左到右的延伸动画
- 箭头从透明到可见
- 持续时间：300ms

### 脉冲效果
- 执行中的节点外发光
- 2秒周期的呼吸效果
- 增强视觉注意力

## 交互功能

### 悬停效果
鼠标悬停在节点上时：
- 节点轻微放大（1.05倍）
- 显示工具提示（如果有）
- 阴影增强
- 层级提升（z-index）

### 点击功能（未来）
计划支持：
- 点击节点查看详细状态
- 点击边查看路由信息
- 右键菜单操作

## 布局算法

当前使用简单的网格布局：
- 节点按顺序排列
- 自动计算列数（基于节点总数）
- 固定间距（150px）

**网格示例**（4个节点）：
```
[node1]  [node2]
[node3]  [node4]
```

**网格示例**（6个节点）：
```
[node1]  [node2]  [node3]
[node4]  [node5]  [node6]
```

## 性能优化

### 节点数量限制
- 推荐：< 20个节点
- 最大：< 50个节点
- 超过50个可能影响性能

### 优化建议
1. **大型Graph** - 考虑分组或简化
2. **长时间执行** - 定期清理旧节点
3. **高频更新** - WebSocket消息可能需要节流

## 故障排除

### 问题1：动态图不显示
**可能原因**：
- JavaScript未加载
- WebSocket未连接
- Graph数据未加载

**解决方法**：
1. 打开浏览器控制台检查错误
2. 刷新页面
3. 检查WebSocket连接状态

### 问题2：节点位置重叠
**可能原因**：
- 节点过多
- 容器太小

**解决方法**：
1. 调整浏览器窗口大小
2. 使用全屏模式
3. 减少同时显示的节点

### 问题3：状态更新延迟
**可能原因**：
- 网络延迟
- WebSocket断开

**解决方法**：
1. 检查网络连接
2. 重新连接WebSocket
3. 刷新页面

### 问题4：动画卡顿
**可能原因**：
- 浏览器性能
- 节点过多

**解决方法**：
1. 关闭其他标签页
2. 使用现代浏览器（Chrome/Firefox）
3. 减少动画效果

## 浏览器兼容性

### 完全支持
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Edge 90+
- ✅ Safari 14+

### 部分支持
- ⚠️ 旧版浏览器可能不支持某些CSS效果

### 不支持
- ❌ IE 11及以下

## 自定义配置

### 修改节点间距
编辑 `dynamic-graph.js`:
```javascript
const nodeSpacing = 150; // 改为你想要的值
```

### 修改颜色方案
编辑 `dynamic-graph.css`:
```css
.dynamic-node[data-status="running"] {
    border-color: #your-color;
    background: #your-gradient;
}
```

### 修改动画速度
编辑 `dynamic-graph.css`:
```css
.dynamic-node {
    transition: all 0.3s ease; /* 改为你想要的时间 */
}
```

## API参考

### DynamicExecutionGraph类

#### 构造函数
```javascript
new DynamicExecutionGraph(containerId)
```

#### 方法

**setGraphData(graphData)**
- 设置Graph结构数据
- 参数：GraphInfo对象

**reset()**
- 清空所有节点和边
- 重置为初始状态

**onNodeStarted(nodeName)**
- 节点开始执行时调用
- 添加或更新节点状态为"running"

**onNodeCompleted(nodeName)**
- 节点执行完成时调用
- 更新节点状态为"completed"
- 添加输出边

**onNodeFailed(nodeName)**
- 节点执行失败时调用
- 更新节点状态为"failed"

## 最佳实践

### 1. 执行前切换
在执行前切换到动态执行图标签，以便从头观察整个流程。

### 2. 清空日志
每次新执行前点击重置按钮，清空之前的状态。

### 3. 合理大小
保持Graph节点数量在合理范围（< 20），以获得最佳体验。

### 4. 网络稳定
确保网络连接稳定，避免WebSocket断开。

### 5. 浏览器选择
使用最新版本的Chrome或Firefox获得最佳性能。

## 未来计划

### v1.1
- [ ] 自动布局算法（力导向图）
- [ ] 节点拖拽功能
- [ ] 缩放和平移

### v1.2
- [ ] 点击节点查看详情
- [ ] 边的标签和说明
- [ ] 执行路径高亮

### v1.3
- [ ] 时间轴回放
- [ ] 导出为图片
- [ ] 自定义主题

### v2.0
- [ ] 3D可视化
- [ ] VR/AR支持
- [ ] 实时协作

## 反馈与支持

如有问题或建议，请：
1. 查看本文档
2. 检查浏览器控制台
3. 提交GitHub Issue
4. 联系开发团队

---

**享受实时动态的Graph执行可视化体验！** 🎉
